# Create a set of colors (duplicates are automatically removed) declared in {curly braces}
colors = {"red", "green", "blue", "red", "yellow"}

print(colors)
# Add an element
colors.add("purple")

print(colors)

# Output is unordered, e.g., {'blue', 'red', 'green', 'yellow'}

# Check for membership quickly
