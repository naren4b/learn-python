# Create a tuple of coordinates (fixed, unchangeable data) declared in (parentheses)
coordinates = (10.0, 20.0)

# Access elements by index
print(f"X:{coordinates[0]}, Y:{coordinates[1]}")

# Note: trying to change a tuple element will cause an error
# coordinates[0] = 15.0 # This would raise a TypeError
